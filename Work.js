import {Typography, Grid} from "@mui/material";

export default function Work() {
  return (
    <Grid container>
        <Grid xs={12} mt={10}>
            <Typography>Work</Typography>
            <Typography>Work</Typography>
            <Typography>Work</Typography>
            <Typography>Work</Typography>
            <Typography>Work</Typography>
            <Typography>Work</Typography>
        </Grid>
    </Grid>
  );
}
