import {Typography, Grid} from "@mui/material";

export default function Skills() {
  return (
    <Grid container>
        <Grid xs={12} mt={10}>
            <Typography>Skills</Typography>
            <Typography>Skills</Typography>
            <Typography>Skills</Typography>
            <Typography>Skills</Typography>
            <Typography>Skills</Typography>
            <Typography>Skills</Typography>
        </Grid>
    </Grid>
  );
}
